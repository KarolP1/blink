import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import SubscriptionLVL from '../../Pages/StarterPage/SubscriptionLevel';

const Subscription = () => {
  return (
    <View>
      <SubscriptionLVL />
    </View>
  );
};

export default Subscription;

const styles = StyleSheet.create({});
